# analyzed_objects
ѕри работе с клиентами и их платежами требуетс€ выполн€ть множество проверок данных на совпадение с определенными объектами.
Ќапример, наличие указанных счетов, номеров карт, email, телефонов, »ЌЌ и пр.
—правочник объектов поиска и анализа предназначен дл€ хранени€ таких объектов. 
ќсновные возможности:
- ведение справочника типов объектов, заведение/редактирование/удаление типов объектов в ручном режиме;
- ведение справочника объектов, взаведение/редактирование/удаление объектов в ручном режиме;
- автоматическое заполнение справочника объектов путем анализа указанной строки и регул€рного выражени€ дл€ поиска;
- анализ произвольной строки на совпадение с объектом из справочника:
	acc407p_utl_graf.check_str(pStr IN VARCHAR2, pType IN VARCHAR2 DEFAULT 'ALL') 
	параметры:
	pStr Ц строка дл€ анализа (назначение платежа, счет получател€);
	pType Ц тип данных (ACC,CARD,..), если не указано Ц то поиск будет по всем типам.